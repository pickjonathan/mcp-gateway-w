Multi-select control. The box fills dark (`--icon-primary`) with a white check when on.

```jsx
<Checkbox label="Enable autoscaling" defaultChecked />
<Checkbox label="Send weekly report" />
<Checkbox label="Locked option" disabled />
```
