Instant on/off switch for settings that apply immediately.

```jsx
<Toggle label="Notifications" defaultChecked />
<Toggle size="sm" hideStateLabel defaultChecked />
```

- On-state track is `--support-success` green. `size` sm/md. Pass `labelOn`/`labelOff` to customize the state text.
