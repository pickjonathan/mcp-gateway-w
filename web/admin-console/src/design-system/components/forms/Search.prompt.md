Search input with a leading magnifier and an auto-appearing clear button.

```jsx
<Search placeholder="Find a resource" onChange={e => setQuery(e.target.value)} />
<Search size="lg" placeholder="Search the catalog" />
```
